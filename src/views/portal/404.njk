{% extends "template/portal.njk" %}

{% block title %}{{ t('error.404.title') }} - Cat Lovers Paradise{% endblock %}

{% block content %}
<div class="container-fluid py-5">
    <div class="row justify-content-center">
        <div class="col-lg-8 text-center">
            <div class="error-template">
                <!-- Imagen de error 404 -->
                <div class="mb-4">
                    <img src="/public/portal/image/404.jpeg" alt="{{ t('error.404.title') }}" class="img-fluid" style="max-width: 400px; border-radius: 15px;" onerror="this.src='/public/portal/image/dummy-img-600x400.jpg'">
                </div>
                
                <h1 class="display-1 text-primary">404</h1>
                <h2 class="mb-4">{{ t('error.404.message') }}</h2>
                
                <div class="alert alert-warning mb-4">
                    <h4>🐱 {{ t('error.404.whatCanYouDo') }}</h4>
                    <p class="mb-0">
                        {{ message or t('error.404.description') }}
                    </p>
                </div>
                
                <div class="row mb-4">
                    <div class="col-md-4">
                        <div class="card border-0 bg-light">
                            <div class="card-body text-center">
                                <i class="fas fa-search fa-2x text-primary mb-2"></i>
                                <h5>{{ t('error.404.searchCats') }}</h5>
                                <p class="small">{{ t('error.404.searchCatsDesc') }}</p>
                            </div>
                        </div>
                    </div>
                    <div class="col-md-4">
                        <div class="card border-0 bg-light">
                            <div class="card-body text-center">
                                <i class="fas fa-home fa-2x text-success mb-2"></i>
                                <h5>{{ t('error.404.explore') }}</h5>
                                <p class="small">{{ t('error.404.aboutUsDesc') }}</p>
                            </div>
                        </div>
                    </div>
                    <div class="col-md-4">
                        <div class="card border-0 bg-light">
                            <div class="card-body text-center">
                                <i class="fas fa-paw fa-2x text-warning mb-2"></i>
                                <h5>{{ t('error.404.contact') }}</h5>
                                <p class="small">{{ t('error.404.contactDesc') }}</p>
                            </div>
                        </div>
                    </div>
                </div>
                
                <div class="d-grid gap-2 d-md-flex justify-content-md-center">
                    <a href="/" class="btn btn-primary btn-lg">
                        <i class="fas fa-home"></i> {{ t('error.404.goHome') }}
                    </a>
                    <a href="/catalog" class="btn btn-outline-primary btn-lg">
                        <i class="fas fa-cat"></i> {{ t('error.404.searchCats') }}
                    </a>
                    <a href="/contact" class="btn btn-outline-secondary btn-lg">
                        <i class="fas fa-envelope"></i> {{ t('error.404.contactUs') }}
                    </a>
                </div>
                
                <div class="mt-4">
                    <div class="alert alert-info">
                        <h5>{{ t('error.404.didYouKnow') }}</h5>
                        <div class="row">
                            <div class="col-md-6">
                                <strong>{{ t('error.404.catsPurr') }}</strong><br>
                                <small>{{ t('error.404.catsPurrDesc') }}</small>
                            </div>
                            <div class="col-md-6">
                                <strong>{{ t('error.404.nightVision') }}</strong><br>
                                <small>{{ t('error.404.nightVisionDesc') }}</small>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>

<style>
.error-template {
    animation: fadeInUp 0.6s ease-out;
}

@keyframes fadeInUp {
    from {
        opacity: 0;
        transform: translateY(30px);
    }
    to {
        opacity: 1;
        transform: translateY(0);
    }
}

.card:hover {
    transform: translateY(-5px);
    transition: transform 0.3s ease;
}
</style>
{% endblock %}