{% extends "template/portal.njk" %}

{% block title %}{{ title }}{% endblock %}

{% block content %}
<!-- Cats Section -->
<section class="section py-5">
    <div class="r-container">
        <div class="row">
            <div class="col-12 text-center mb-5">
                <h1 class="display-4 fw-bold mb-4">Gatos Disponibles</h1>
                <p class="lead">Encuentra a tu compañero felino perfecto</p>
            </div>
        </div>
        
        <!-- Filters -->
        <div class="row mb-5">
            <div class="col-12">
                <div class="card">
                    <div class="card-body">
                        <h5 class="card-title">Filtros de Búsqueda</h5>
                        <form method="GET" class="row g-3">
                            <div class="col-md-4">
                                <label for="breed" class="form-label">Raza</label>
                                <select class="form-select" id="breed" name="breed">
                                    <option value="">Todas las razas</option>
                                    <option value="sphynx" {% if breed == 'sphynx' %}selected{% endif %}>Sphynx</option>
                                    <option value="bambino" {% if breed == 'bambino' %}selected{% endif %}>Bambino</option>
                                    <option value="persa">Persa</option>
                                    <option value="siames">Siamés</option>
                                    <option value="maine-coon">Maine Coon</option>
                                </select>
                            </div>
                            <div class="col-md-4">
                                <label for="age" class="form-label">Edad</label>
                                <select class="form-select" id="age" name="age">
                                    <option value="">Todas las edades</option>
                                    <option value="0-1" {% if age == '0-1' %}selected{% endif %}>0-1 año</option>
                                    <option value="1-3" {% if age == '1-3' %}selected{% endif %}>1-3 años</option>
                                    <option value="3-5" {% if age == '3-5' %}selected{% endif %}>3-5 años</option>
                                    <option value="5+" {% if age == '5+' %}selected{% endif %}>5+ años</option>
                                </select>
                            </div>
                            <div class="col-md-4 d-flex align-items-end">
                                <button type="submit" class="btn btn-primary w-100">
                                    <i class="fa-solid fa-search me-2"></i>Buscar
                                </button>
                            </div>
                        </form>
                    </div>
                </div>
            </div>
        </div>
        
        <!-- Cats Grid -->
        <div class="row">
            <div class="col-12 text-center">
                <div class="alert alert-info" role="alert">
                    <h4 class="alert-heading">¡Próximamente!</h4>
                    <p>Estamos trabajando en nuestro catálogo de gatos. Muy pronto podrás ver todos los gatos disponibles para adopción.</p>
                    <hr>
                    <p class="mb-0">¿Tienes preguntas? <a href="/contact" class="alert-link">Contáctanos</a></p>
                </div>
            </div>
        </div>
    </div>
</section>
{% endblock %}
